# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
  
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Nodejs
        uses: actions/setup-node@v3
        with:
          node-version: 16
      - name: Install Dependencies
        run: npm install

      - name: Build All apps
        run: npm run build
      
  detect-example1-changed:
    runs-on: ubuntu-latest
    name: detect if example1 needs to deployed
    steps:
      - name: Check changes in example2
        uses: dorny/paths-filter@v2
        id: example1Changes
        with:
          filters: |
            example1:
              - 'apps/example1/**'

  deploy-example1:
    if: needs.detect-example1-changed.outputs.example1 == 'true'
    needs: detect-example1-changed
    name: Deploy example1
    runs-on: ubuntu-latest
    steps:
      - name: Deploy example1
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          entryPoint: "./apps/example1"
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_VERSION_CONTROL_91D8F }}'
          projectId: version-control-91d8f
          channelId: ''

  detect-example2-changed:
    runs-on: ubuntu-latest
    name: detect if example2 needs to deployed
    steps:
      - name: Check changes in example2
        uses: dorny/paths-filter@v2
        id: example2Changes
        with:
          filters: |
            example2:
              - 'apps/example2/**'

  deploy-example2:
    if: needs.detect-example2-changed1.outputs.example2 == 'true'
    needs: detect-example2-changed
    name: Deploy example2
    runs-on: ubuntu-latest
    steps:
      - name: Deploy example2
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          entryPoint: "./apps/example2"
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_VERSION_CONTROL_91D8F }}'
          projectId: version-control-91d8f
          channelId: ''



